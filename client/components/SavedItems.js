import React, { Component } from 'react';

export default class SaveItems extends Component {
  //component on mount makes axios call getting saved items
  render() {
    return <h1>Saved Items</h1>;
    // display saved items similar to messages, add a jump to comment button
  }
}
